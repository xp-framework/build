package net.xp_framework.build.subscriber;

import lang.archive.Archive;
import io.Folder;

/**
 * A XAR release finalizer gets called once all archives have been created.
 *
 * @see  xp://net.xp_framework.build.subscriber.DefaultFinalizer
 */
public interface XarReleaseFinalizer {

  /**
   * Finalize
   */
  public void finalize([:var] $build, [:Archive] $archives, Folder $targetDir);
}